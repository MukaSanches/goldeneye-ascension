name: Modern Controls V3 validation

on:
  push:
    branches: [ ascension-modern-controls-v3 ]
  pull_request:
    branches: [ ascension-modern-controls-v2, ascension-modern-controls-v3 ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: modern-controls-v3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux-gcc14-v3:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc-14 g++-14 cmake python3 \
            libsdl2-dev zlib1g-dev libgl1-mesa-dev

      - name: Python syntax
        run: python3 -m compileall -q tools_pc

      - name: Apply V2 baseline
        run: |
          python3 tools_pc/apply_modern_controls_v2.py --check
          python3 tools_pc/apply_modern_controls_v2.py --no-backup

      - name: V3 fail-closed preflight and apply
        run: |
          python3 tools_pc/apply_modern_controls_v3.py --check
          python3 tools_pc/apply_modern_controls_v3.py --no-backup
          python3 tools_pc/apply_modern_menu_guard.py --check
          python3 tools_pc/apply_modern_menu_guard.py
          python3 tools_pc/apply_modern_menu_guard.py --check

      - name: V3 randomized and regression tests
        run: |
          python3 tools_pc/test_modern_controls_v2.py
          python3 tools_pc/test_modern_controls_v3.py
          python3 tools_pc/verify_ascension_contracts.py
          git diff --check

      - name: Regenerate ROM symbols
        run: python3 scripts/gen_romassets.py u

      - name: Configure GCC 14
        run: cmake -S . -B build-v3-gcc14 -DROMID=ntsc-final -DCMAKE_C_COMPILER=gcc-14 -DCMAKE_CXX_COMPILER=g++-14

      - name: Build GCC 14
        run: cmake --build build-v3-gcc14 -j"$(nproc)"

      - name: Verify GCC 14 binary
        run: |
          test -f build-v3-gcc14/ge007.x86_64
          file build-v3-gcc14/ge007.x86_64

  linux-gcc13-v3:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-13 g++-13 cmake python3 libsdl2-dev zlib1g-dev libgl1-mesa-dev

      - name: Apply V2 + V3 + menu guard
        run: |
          python3 tools_pc/apply_modern_controls_v2.py --check
          python3 tools_pc/apply_modern_controls_v2.py --no-backup
          python3 tools_pc/apply_modern_controls_v3.py --check
          python3 tools_pc/apply_modern_controls_v3.py --no-backup
          python3 tools_pc/apply_modern_menu_guard.py --check
          python3 tools_pc/apply_modern_menu_guard.py
          python3 tools_pc/apply_modern_menu_guard.py --check
          python3 tools_pc/test_modern_controls_v2.py
          python3 tools_pc/test_modern_controls_v3.py
          python3 tools_pc/verify_ascension_contracts.py
          git diff --check

      - name: Regenerate ROM symbols
        run: python3 scripts/gen_romassets.py u

      - name: Configure GCC 13
        run: cmake -S . -B build-v3-gcc13 -DROMID=ntsc-final -DCMAKE_C_COMPILER=gcc-13 -DCMAKE_CXX_COMPILER=g++-13

      - name: Build GCC 13
        run: cmake --build build-v3-gcc13 -j"$(nproc)"

      - name: Verify GCC 13 binary
        run: |
          test -f build-v3-gcc13/ge007.x86_64
          file build-v3-gcc13/ge007.x86_64

  windows-v3:
    runs-on: windows-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 1

      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-SDL2
            mingw-w64-x86_64-zlib
            mingw-w64-x86_64-python

      - name: Python syntax
        run: python -m compileall -q tools_pc

      - name: Apply V2 + V3 + menu guard and run randomized contracts
        run: |
          python tools_pc/apply_modern_controls_v2.py --check
          python tools_pc/apply_modern_controls_v2.py --no-backup
          python tools_pc/apply_modern_controls_v3.py --check
          python tools_pc/apply_modern_controls_v3.py --no-backup
          python tools_pc/apply_modern_menu_guard.py --check
          python tools_pc/apply_modern_menu_guard.py
          python tools_pc/apply_modern_menu_guard.py --check
          python tools_pc/test_modern_controls_v2.py
          python tools_pc/test_modern_controls_v3.py

      - name: Regenerate ROM symbols
        run: python scripts/gen_romassets.py u

      - name: Build Windows
        run: ./build-pc.sh ntsc-final

      - name: Verify executable
        run: test -f build-pc/ge007.x86_64.exe
